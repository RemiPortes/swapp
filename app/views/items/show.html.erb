<%= @item %><br/>
<%= @possible_match %><br/>

<%= form_for(@dislike) do |f| %>
	<%= f.hidden_field :user_id %>
	<%= f.hidden_field :user_item_id %>
	<%= f.hidden_field :item_id %>
	<%= f.hidden_field :value %>
	<%= f.submit 'NON' %>
<% end %>

<%= form_for(@like) do |f| %>
	<%= f.hidden_field :user_id %>
	<%= f.hidden_field :user_item_id %>
	<%= f.hidden_field :item_id %>
	<%= f.hidden_field :value %>
	<%= f.submit 'OUI' %>
<% end %>
